# emoji-mart's React 18 peer range conflicts with this project's React 19.
# Both are compatible at runtime; pin legacy resolution so installs (and the
# Vercel build) don't fail on the pre-existing ERESOLVE.
legacy-peer-deps=true
